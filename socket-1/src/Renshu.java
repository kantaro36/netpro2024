public class Renshu {
    // xを2倍にして返す関数
    public int doubleValue(int x) {
        return x * 2;
    }

    //ここに続きを実装していく。

    public int sumUpToN(int x) {
        return (x / 2) * (x + 1);
    }

    public int sumFromPtoQ(int x, int y){
        return (y / 2) * (x + y);
    }
}
